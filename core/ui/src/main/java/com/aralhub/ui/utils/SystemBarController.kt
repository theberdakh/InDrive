package com.aralhub.ui.utils

/**
 * SystemBar Co*/
interface SystemBarController {
    fun showLightStatusBar(iconColor: Int, iconBackgroundColor: Int)
    fun showDarkStatusBar(iconBackgroundColor: Int)
}